<Page
    x:Class="MiLED.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MiLED"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Viewbox StretchDirection="Both" Stretch="Fill">
            <ToggleButton x:Name="SwitchButton" Click="SwitchButton_Click" BorderThickness="0" Margin="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Padding="100">
                <Grid>
                    <TextBlock Text="" FontFamily="Segoe UI Symbol" FontSize="400" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                    <Ellipse Stroke="{ThemeResource BackButtonForegroundThemeBrush}" StrokeThickness="30" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                </Grid>
            </ToggleButton>
        </Viewbox>
    </Grid>
</Page>
